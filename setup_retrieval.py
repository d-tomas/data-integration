from setuptools import setup

setup(name = 'table_retrieval',
    version = '0.1',
    description = 'Given the similarity between CSV tables, retrieve the top "n" tables for a given table.',
    author = 'David Tomás',
    author_email = 'dtomas@dlsi.ua.es',
    packages = ['table_retrieval'])
