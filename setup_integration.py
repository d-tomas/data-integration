from setuptools import setup

setup(name = 'table_integration',
    version = '0.1',
    description = 'Given the similarity between CSV tables, identify which tables can be integrated by means of union and join operations.',
    author = 'David Tomás',
    author_email = 'dtomas@dlsi.ua.es',
    packages = ['table_integration'])
